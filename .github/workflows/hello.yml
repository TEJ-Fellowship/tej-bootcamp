name: Hello World!

on:
  push:
    branches:
      - part11-b

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
      - name: Listing the files
        run: |
          ls -l
